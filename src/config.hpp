#pragma once // should be included only inside common.hpp!

////////////////////////////////////////////////////////////////////////////////
// Main configuration

#define DEBUG LEVEL_DEBUG

USE_LOG_LEVEL_DEFAULT(LEVEL_DEBUG);
USE_LOG_LEVEL(Time, LEVEL_TRACE);

////////////////////////////////////////////////////////////////////////////////
